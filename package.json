{
  "name": "result-software",
  "version": "1.0.0",
  "description": "Result Software - Student Result Management System",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "npm --prefix backend run dev & npm --prefix frontend-react run dev",
    "build": "npm --prefix frontend-react run build",
    "install-all": "npm install && npm --prefix backend install && npm --prefix frontend-react install"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.11.3",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.37.8",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
